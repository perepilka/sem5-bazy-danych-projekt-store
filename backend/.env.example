# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=store
DB_USERNAME=storeuser
DB_PASSWORD=changeme

# Application Configuration
SERVER_PORT=8080

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production-make-it-at-least-256-bits
JWT_EXPIRATION=86400000

# Employee Domain (for subdomain routing)
EMPLOYEE_DOMAIN=employee.store.com
CUSTOMER_DOMAIN=store.com
