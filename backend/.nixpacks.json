{
  "providers": ["java"],
  "phases": {
    "setup": {
      "nixPkgs": ["jdk21", "maven"]
    },
    "build": {
      "cmds": ["./mvnw clean package -DskipTests"]
    },
    "start": {
      "cmd": "java -Dserver.port=$PORT -jar target/store-0.0.1-SNAPSHOT.jar"
    }
  }
}
